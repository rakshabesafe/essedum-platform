{
  "root": true,
  "comment": "Plan §A.7 — block cross-MFE imports. Activates when ESLint is installed.",
  "rules": {
    "no-restricted-imports": ["error", {
      "patterns": [
        {
          "group": [
            "**/apps/agent/**",
            "**/apps/data-ops/**",
            "**/apps/integration/**"
          ],
          "message": "Cross-MFE imports are forbidden. Use @essedum/shared-lib or the event bus."
        }
      ]
    }]
  }
}
