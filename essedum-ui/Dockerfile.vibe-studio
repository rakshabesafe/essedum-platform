# Build context: essedum-platform/ (repo root)
FROM node:22-alpine AS build

WORKDIR /workspace
COPY essedum-ui/shell/ ./shell/
WORKDIR /workspace/shell
RUN npm install --legacy-peer-deps --force

COPY essedum-ui/modules/vibe-studio/ /workspace/modules/vibe-studio/
WORKDIR /workspace/modules/vibe-studio
RUN rm -f package-lock.json yarn.lock
RUN npm install --legacy-peer-deps --force
RUN npm run build-prod

FROM nginx:alpine
COPY essedum-ui/nginx_mfe.conf /etc/nginx/nginx.conf
WORKDIR /app/ui
COPY --from=build /workspace/modules/vibe-studio/dist/vibe-studio /app/ui/
RUN chmod -R 755 /app/ui

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
