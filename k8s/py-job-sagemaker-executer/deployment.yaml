apiVersion: apps/v1
kind: Deployment
metadata:
  name: py-job-sagemaker-executer
  namespace: essedum
  labels:
    app: py-job-sagemaker-executer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: py-job-sagemaker-executer
  template:
    metadata:
      labels:
        app: py-job-sagemaker-executer
    spec:
      nodeSelector:
        kubernetes.io/hostname: essedum-1
      containers:
        - name: py-job-sagemaker-executer
          image: essedum/py-job-sagemaker-executer:latest
          imagePullPolicy: Never
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5002
              name: http
          envFrom:
            - configMapRef:
                name: essedum-config
            - secretRef:
                name: essedum-secret
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "2Gi"
          livenessProbe:
            exec:
              command:
                - bash
                - -c
                - "echo > /dev/tcp/127.0.0.1/5002"
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 5
          readinessProbe:
            exec:
              command:
                - bash
                - -c
                - "echo > /dev/tcp/127.0.0.1/5002"
            initialDelaySeconds: 15
            periodSeconds: 10
