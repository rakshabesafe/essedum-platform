apiVersion: v1
kind: Service
metadata:
  name: langfuse-postgres
  namespace: essedum
  labels:
    app: langfuse-postgres
spec:
  selector:
    app: langfuse-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
