apiVersion: v1
kind: Service
metadata:
  name: langfuse-worker
  namespace: essedum
  labels:
    app: langfuse-worker
spec:
  selector:
    app: langfuse-worker
  ports:
    - name: http
      port: 3030
      targetPort: 3030
  type: ClusterIP
