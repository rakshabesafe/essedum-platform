apiVersion: v1
kind: Service
metadata:
  name: litellm-postgres
  namespace: essedum
  labels:
    app: litellm-postgres
spec:
  selector:
    app: litellm-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
