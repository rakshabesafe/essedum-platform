apiVersion: v1
kind: Service
metadata:
  name: langflow-persistent
  namespace: aipns
  labels:
    app: langflow-persistent
spec:
  type: ClusterIP
  selector:
    app: langflow-persistent
  ports:
  - name: http
    protocol: TCP
    port: 7860
    targetPort: 7860
  sessionAffinity: None
