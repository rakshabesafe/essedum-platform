apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.qdrant.service.name }}
spec:
  selector:
    app: qdrant
  ports:
    - protocol: TCP
      port: {{ .Values.qdrant.service.port }}
      targetPort: {{ .Values.qdrant.service.targetPort }}
  type: LoadBalancer
