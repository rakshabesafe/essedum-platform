# Slim requirements for local development / smoke testing
# Uses SQLite (aiosqlite) instead of PostgreSQL

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
h11==0.14.0
httpcore==1.0.8
colorama==0.4.4       # 0.4.5 and 0.4.6 are 403 in Artifactory
click==7.1.2          # 8.x requires colorama>=0.4.6 which is 403 blocked

# Database (SQLite for local, PostgreSQL in prod)
sqlalchemy[asyncio]>=2.0.31
aiosqlite>=0.20.0
alembic>=1.13.0

# Validation & settings
pydantic>=2.8.0
pydantic-settings>=2.3.0

# Cache (optional — app falls back gracefully when not available)
redis[asyncio]>=5.0.0

# Vector DB - Qdrant (requires separate install when Qdrant server is available)
# qdrant-client>=1.10.0   # Excluded: pulls hyperframe 6.1.0 which is 403 in Artifactory

# Model providers (install only what you need)
openai>=1.35.0            # Azure OpenAI
httpx>=0.27.0,<0.29.0
httpx-sse>=0.4.0

# MCP client
mcp>=1.4.1

# Document parsing
beautifulsoup4>=4.12.0

# Chunking - native implementation (no langchain required for local dev)
